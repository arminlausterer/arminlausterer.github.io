---
label: Header
hide_body: true
fields:
- name: logo
  type: file
  config:
    maxSize: 2
  label: logo
- name: livecall
  type: field_group
  config: {}
  fields:
  - name: livecall
    type: field_group_list
    fields:
    - name: bild
      type: file
      config:
        maxSize: 64
      label: Bild
    - name: buttonbeschriftung
      type: text
      config:
        required: false
      label: buttonbeschriftung
    - name: buttonlink
      type: textarea
      default: ''
      config:
        required: false
        wysiwyg: true
        schema:
          format: html-blocks
      label: buttonlink
    config:
      min: 
      max: 
      labelField: 
    label: livecall
  label: Livecall
- name: demo
  type: field_group
  config: {}
  fields:
  - name: demo
    type: field_group_list
    fields:
    - name: bild
      type: file
      config:
        maxSize: 64
      label: Bild
    - name: buttonbeschriftung
      type: text
      config:
        required: false
      label: buttonbeschriftung
    - name: buttonlink
      type: textarea
      default: ''
      config:
        required: false
        wysiwyg: true
        schema:
          format: html-blocks
      label: buttonlink
    config:
      min: 
      max: 
      labelField: 
    label: demo
  label: demo
- name: top100
  type: file
  config:
    maxSize: 64
  label: top100
pages:
- content/global/header.md
