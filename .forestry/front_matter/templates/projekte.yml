---
label: Projekte
hide_body: true
fields:
- name: header
  type: text
  config:
    required: false
  label: Header
- name: kunden
  type: field_group_list
  fields:
  - name: bild
    type: file
    config:
      maxSize: 64
    label: Bild
  - name: kunde
    type: text
    config:
      required: false
    label: Kunde
  - name: beschreibung
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: Beschreibung
  - name: buttonbeschriftung
    type: text
    config:
      required: false
    label: buttonbeschriftung
  - name: buttonlinkfile
    type: file
    config:
      maxSize: 64
    label: buttonlinkfile
  config:
    min: 
    max: 
    labelField: 
  label: Kunden
- name: seo
  type: include
  config: {}
  template: seo
  label: seo
pages:
- content/Projekte.md
